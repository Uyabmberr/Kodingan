type Protect = {
    path: string;
    /**
     * The HTTP method to protect.
     * 'GET', 'POST', 'PUT', 'DELETE', '*', ...
     */
    method: string;
    advancedOptions?: {
        checkLevel?: 'deepAnalysis' | 'basic';
    };
};

interface Challenge {
    b: number;
    v: string;
    e: string;
    d: 1 | 0;
}
declare global {
    interface Window {
        KPSDK: any;
        V_C?: Challenge[];
        IS_HUMAN_INITIALIZED?: boolean;
        _KPSDK_LOAD_PROMISE?: Promise<void> | null;
        pRoutes?: Protect[];
    }
}
declare function initBotId({ protect }: {
    protect: Protect[];
}): void;

declare const validateProtectedRoutes: (protect: Protect[]) => Protect[];

export { initBotId, validateProtectedRoutes };
